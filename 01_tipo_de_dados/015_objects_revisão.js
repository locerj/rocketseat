// Object

// const person = {} //objeto criado vazio

const person = {
    name: 'John',
    age: 30,
    wheight: 88.6,
    isAdmin: true
}

console.log(person) // pegando o objeto inteiro
console.log(person.name) // pegando um atributo do objeto
console.log(`${person.name} tem ${person.age}`)